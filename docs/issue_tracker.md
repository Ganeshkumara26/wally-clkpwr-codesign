# Issue Tracker — Real Engineering Decisions

| ID | Date | Description | Options Considered | Decision & Rationale | Evidence | Status |
|---|---|---|---|---|---|---|
| ISS-001 | Month 1 | Wally uses advanced SystemVerilog (packages, structs). Full-core simulation requires Verilator or commercial tools. iverilog can't parse it. | 1. Wait for Docker/OpenLane setup. 2. Work module-by-module: extract individual Wally modules (regfile, ALU) and port to Verilog-2005 for iverilog. 3. Abandon Wally. | **Option 2.** The register file and ALU are the gating targets anyway. Port them to iverilog-compatible Verilog while keeping functionally identical logic. This lets us run real simulations NOW. | `regfile.v` compiled and simulated successfully with iverilog. | Resolved |
| ISS-002 | Month 1 | ICG cell uses wrong latch polarity. Standard ICG (neg-level latch) is designed for posedge-triggered FFs. Wally's regfile uses `always @(negedge clk)`. | 1. Invert latch polarity to positive-level-sensitive. 2. Change regfile to posedge (breaks Wally pipeline contract). 3. Use a different gating topology. | **Option 1.** Created `icg_cell_neg.v` with `if (clk_in)` latch (transparent when HIGH, holds when LOW). This lets `we3` propagate during HIGH phase so the gated falling edge arrives correctly. | v002 FAILED (16 errors). v003 with inverted polarity PASSED (0 errors). Debug trace in `sim/v003_icg_fixed/debug.vcd`. | Resolved |
| ISS-003 | Month 1 | Testbench was driving `we3` at `@(negedge clk)` — the same edge the regfile uses for writes. With ICG, the latch needs `we3` stable during the HIGH phase. | 1. Fix testbench timing to drive at `@(posedge clk)`. 2. Add pipeline delay register for we3. | **Option 1.** In a real 5-stage pipeline, `RegWriteW` is computed by combinational decode logic and stable before the clock edge. The testbench should mimic this. Driving at posedge gives the ICG latch a full half-cycle to capture the enable. | v003 tb_regfile_icg.v drives at posedge, PASS 0 errors. | Resolved |
| ISS-004 | Month 1 | Yosys 0.9 cannot parse SystemVerilog packages (`import cvw::*`). Full-core synthesis blocked. | 1. Use Yosys with Surelog/UHDM plugin. 2. Synthesize individual modules in Verilog-2005. 3. Install newer Yosys. | **Option 2.** Synthesize the regfile and ICG modules individually with Yosys. The cell counts and FF counts are accurate for the register file domain, which is our primary gating target. | Yosys baseline: 5088 cells, 992 DFF_N. ICG: 5092 cells (+0.08%), 1 DLATCH_P, 1 AND added. | Resolved |
| ISS-005 | Month 2 | ALU consumes dynamic power during inactive cycles (LOAD/STORE/BRANCH) due to operands changing and rippling through the 32-bit combinational adder/shifter cloud. | 1. Latch the inputs when inactive. 2. AND-clamp inputs to zero when inactive (`alu_active` signal). 3. OAI (OR-AND-Invert) specific sub-blocks. | **Option 2.** Latching adds large area (64 FFs). AND-clamping uses minimal area (64 AND gates) and immediately zeroes the combinational tree, preventing downstream rippling. Created `alu_isolated.v`. | Testbench `tb_alu.v` shows 93% toggle reduction during 50 inactive cycles with only +6.7% cell area overhead. Functional equivalence passed. | Resolved |
| ISS-006 | Month 3 | ALU Isolation breaks `AUIPC` instruction. Naive integration logic failed to set `alu_active` for U-type instructions, causing inputs to clamp to 0. | 1. Disable isolation entirely. 2. Update `alu_active` decoder to specifically include `OP_AUIPC` and `OP_LUI`. | **Option 2.** Updated `execute_stage.v` to assert `alu_active` for U-type instructions. | Testbench `tb_execute.v` now perfectly calculates `12346000` for AUIPC. Bug resolved. | Resolved |
